<form action=<%= $prefix + @apitoken + "/tweet/new" %> method="POST">
  <div class="form-group">
    <input class="form-control" type="text" name="tweet[content]" placeholder="What's happening?">
  </div>
  <button type="submit" class="btn btn-primary"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Tweet</button>
</form>
